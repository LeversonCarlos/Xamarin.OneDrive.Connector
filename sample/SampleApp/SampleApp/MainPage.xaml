<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
   xmlns="http://xamarin.com/schemas/2014/forms"
   xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
   xmlns:local="clr-namespace:SampleApp"
   x:Class="SampleApp.MainPage">

   <StackLayout VerticalOptions="Center" Margin="20">
      <Label x:Name="InfoLabel" HorizontalOptions="Center" />
      <Button x:Name="ConnectButton" HorizontalOptions="Center" Clicked="ConnectButton_Clicked" />
      <Button x:Name="DisconnectButton" HorizontalOptions="Center" Clicked="DisconnectButton_Clicked" />

      <Button x:Name="FilesButton" HorizontalOptions="Center" Text="Load Files" Clicked="FilesButton_Clicked" />
      <Image x:Name="ImageCover" Aspect="AspectFit" HeightRequest="150" />
      <ListView x:Name="FileList" HorizontalOptions="Center" HeightRequest="500">
         <ListView.ItemTemplate>
            <DataTemplate>
               <TextCell Text="{Binding FileName}" Detail="{Binding FilePath}" Tapped="ImageCell_Tapped" />
            </DataTemplate>
         </ListView.ItemTemplate>
      </ListView>

   </StackLayout>

</ContentPage>